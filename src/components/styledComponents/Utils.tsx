import { styled } from '@mui/system';

export const CenteredContainer = styled('div')(() => ({
    position: 'absolute',
    margin: 'auto',
    top: '0',
    left: '0',
    right: '0',
    bottom: '0',
}));
